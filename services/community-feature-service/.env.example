PORT=3006

# SQL DB 1 — Car Configurator Service (active configurations, via SQL API)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=sideboard123
DB_NAME=sideboard_db
DB_PORT=3306

# SQL DB 2 — Sideboard-Konfigurator DB (saved sideboards, via SQL API)
# In production: point to a separate MySQL instance
DB_SAVED_HOST=localhost
DB_SAVED_USER=root
DB_SAVED_PASSWORD=sideboard123
DB_SAVED_NAME=sideboard_db

# MinIO — Picture Store (binary images only; metadata goes to MySQL)
MINIO_HOST=localhost
MINIO_PORT=9000
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
MINIO_BUCKET=sideboard

SESSION_SECRET=mein-geheimes-session-secret
